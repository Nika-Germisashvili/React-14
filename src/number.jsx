import React from 'react';

const Number = (props) => {
    return (
        <div class="p-3">
            <p class="fs-1">{props.number}</p>
        </div>
    );
};

export default Number;